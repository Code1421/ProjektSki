@page
@model ProjektSki.Pages.Producers.DetailsModel

@{
    ViewData["Title"] = "Informacje o producencie";
}

<h1>Informacje</h1>

<div>
    <h4>Producent</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Producer.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Producer.Name)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Producer.Country)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Producer.Country)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.Producer.Id">Edytuj</a> |
    <a asp-page="./Index">Powrót</a>
</div>
